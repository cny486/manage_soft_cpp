# 服务端更新说明

## 能不能直接用现有部署脚本

可以。

项目里的 `scripts/deploy_linux_server.ps1` / `scripts/deploy_linux_server.py` 本质上做的是：

1. 把当前源码打包上传到 Linux 服务器
2. 在服务器上只编译 `ManageSoftServer`
3. 用新二进制替换 `run/ManageSoftServer`
4. 重启 `systemd` 服务

它清理的是部署工作目录里的 `src/` 和 `build/`，不会主动删除服务端业务数据。

## 数据会不会丢

正常不会。

服务端数据不放在部署目录里，而是放在 Qt 的应用数据目录下。当前服务端代码里：

- 应用名：`ManageSoftServer`
- 组织名：`ManageSoftCpp`
- 存储目录：`QStandardPaths::AppDataLocation/data`

在 Linux 上，默认通常会落到类似下面的位置：

```text
~/.local/share/ManageSoftCpp/ManageSoftServer/data
```

也就是说，只要你是“更新程序本体”，不是去手动删除这个 `data` 目录，原有库存、需求库、附件等数据都会保留。

## 更稳妥的一键更新方式

我已经补了一个带自动备份的数据安全版脚本：

- `scripts/deploy_linux_server_safe.ps1`

它会在重启服务前，先把服务端数据目录打成备份包，再继续部署。

默认备份目录：

```text
~/manage_soft_backups
```

## 推荐用法

```powershell
./scripts/deploy_linux_server_safe.ps1 `
  -ServerHost 111.229.149.41 `
  -Username lws2 `
  -RemoteRoot '~/manage_soft_cpp_linux_build' `
  -ServiceName manage-soft-server `
  -ListenHost 0.0.0.0 `
  -ListenPort 45454
```

如果你想直接传密码：

```powershell
./scripts/deploy_linux_server_safe.ps1 `
  -ServerHost 111.229.149.41 `
  -Username lws2 `
  -Password '你的密码' `
  -RemoteRoot '~/manage_soft_cpp_linux_build'
```

## 什么时候需要手动指定数据目录

如果你的服务不是以默认用户目录运行，或者你以前改过 Qt 数据目录，可以额外指定：

```powershell
./scripts/deploy_linux_server_safe.ps1 `
  -ServerHost 111.229.149.41 `
  -Username lws2 `
  -RemoteDataRoot '/home/lws2/.local/share/ManageSoftCpp/ManageSoftServer/data'
```

## 更新前后建议检查

更新前：

- 确认服务器当前服务名是否真的是 `manage-soft-server`
- 确认登录用户和 `systemd` 里的运行用户一致

更新后：

- `sudo systemctl status manage-soft-server`
- `sudo journalctl -u manage-soft-server -n 100 --no-pager`

服务端启动日志里会打印一行：

```text
Storage root: ...
```

这行就是当前实际数据目录。第一次更新时，建议你核对一下它是不是你预期的路径。
## 2026-06-22 更新记录

- 变更内容：
  - 配单分析改为优先按配单表中实际已填写的关键列与库存对应列比对。
  - `manufacturerPart` 保持严格匹配。
  - 其他关键列支持“库存字段包含配单字段”即命中，适用于 `10nF` 命中 `10nF+-10%` 这类场景。
- 影响范围：
  - 该逻辑运行在服务端 `JsonStorageService`，TCP 模式下必须重新部署服务器。
- 推荐部署命令：

```powershell
./scripts/deploy_linux_server_safe.ps1 `
  -ServerHost 111.229.149.41 `
  -Username lws2 `
  -RemoteRoot '~/manage_soft_cpp_linux_build' `
  -ServiceName manage-soft-server `
  -ListenHost 0.0.0.0 `
  -ListenPort 45454
```

- 若服务器数据目录不是默认值，额外补上：

```powershell
-RemoteDataRoot '/home/lws2/.local/share/ManageSoftCpp/ManageSoftServer/data'
```

- 实际部署结果：
  - 已通过 `scripts/deploy_linux_server_safe.ps1` 部署到 `111.229.149.41`
  - 服务名：`manage-soft-server`
  - 服务状态：`active (running)`
  - 监听地址：`0.0.0.0:45454`
  - 数据目录：`/home/lws2/.local/share/ManageSoftCpp/ManageSoftServer/data`
  - 本次重启前备份：`/home/lws2/manage_soft_backups/manage_soft_data_20260622_231145.tar.gz`

- 同日补丁：
  - 修正“配单一个结果都搜不到”的问题。
  - 原因是候选过滤过严，只要某个已填字段不命中就整条库存被排除。
  - 现已改为：按对应列直接比较，命中的列参与加分排序；只要至少命中一个关键列即可进入候选列表。
  - `manufacturerPart` 仍然是最高优先级，且唯一精确命中时仍会自动确认。
  - 已再次部署到服务器，重启前备份：`/home/lws2/manage_soft_backups/manage_soft_data_20260622_231811.tar.gz`
