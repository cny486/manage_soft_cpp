# 配单使用说明

## 1. 适用入口

当前软件中的配单流程有两个入口：

1. 库存管理页中的 `配单`
2. 需求库详情页中的 `按制造数配单`

两者都会进入同一套“配单确认”界面。

## 2. 配单表格式

### 最低要求

配单表至少需要包含：

1. `Quantity`
2. 至少一个识别字段

可用的识别字段包括：

- `Manufacturer Part`
- `Name`
- `Value`
- `Footprint`
- `特征值`
- `Manufacturer`
- `Device`
- `Category`

### 推荐列

为了提高匹配准确度，建议尽量提供以下列：

- `Manufacturer Part`
- `Name`
- `Value`
- `Footprint`
- `特征值`
- `Manufacturer`
- `Device`
- `Category`
- `Designator`
- `Note`

### 说明

- `Quantity` 必须是大于 `0` 的整数。
- 如果某一行没有任何可用识别信息，该行会被忽略。
- 如果某一行数量格式错误，该行会被忽略。

## 3. 匹配规则

系统会优先按 `Manufacturer Part` 做精确匹配。

如果没有可用的 `Manufacturer Part`，或者无法唯一命中，系统会综合以下信息做候选匹配：

- `Name`
- `Value`
- `Footprint`
- `特征值`
- `Manufacturer`
- `Device`
- `Category`
- `Designator`

系统会为每条需求生成最多 `4` 个候选库存项，并按匹配度排序。

## 4. 配单确认界面

界面分为两部分：

1. 上方是需求项结果表
2. 下方是当前需求项的候选库存列表

### 上方结果表

会显示：

- 当前状态
- 需求项摘要
- 需求数量
- 当前匹配结果
- 匹配度
- 可用库存
- 确认状态
- 来源行

状态说明：

- `已确认且库存充足`
- `已确认但库存不足`
- `候选待确认`
- `无匹配候选`

### 下方候选列表

当你点击上方某一条需求时，下方会显示它的候选库存项。

每个候选项会显示：

- 候选库存物料摘要
- 匹配度
- 命中字段
- 可用库存
- 单位
- 库位

你可以：

1. 点击 `选用此项`
2. 或直接双击候选行

确认后，上方该需求项会更新为已确认状态。

如果想撤销当前选择，可以点击 `取消当前确认`。

## 5. 一键出库

`一键出库已确认项` 按钮位于配单确认窗口的右下角。

它只会对以下条目生效：

1. 已人工确认
2. 当前库存充足

以下条目不会执行出库：

- 候选待确认
- 无匹配候选
- 已确认但库存不足

## 6. 导出结果

点击 `导出匹配结果` 可以导出当前配单确认结果。

导出的内容包括：

- 配单状态
- 确认状态
- 需求项
- Manufacturer Part
- 需求数量
- 可用库存
- 缺口数量
- 当前匹配库存物料
- 匹配度
- 命中字段
- Manufacturer
- Name
- 单位
- 存储位置
- 来源文件
- 来源行

## 7. 建议使用方式

建议按下面顺序操作：

1. 导入配单表
2. 查看哪些条目是 `候选待确认`
3. 逐项选择最合适的库存候选
4. 检查是否还有 `库存不足` 或 `无匹配候选`
5. 点击右下角 `一键出库已确认项`
6. 如有需要，再导出匹配结果留档
